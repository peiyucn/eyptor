# Spec：v1.2 大型体验优化批次

> 创建日期：2026-09-04
> 状态：草案（待确认）
> 范围：epytor v1.2.x 的编辑体验修复 + 上游能力吸收 + 友商功能借鉴

## 背景与现状基线

- 当前版本 1.1.6，Milkdown 7.22.0（npm 最新 7.22.1，2026-08-12 发布）
- 调研证据：Milkdown CHANGELOG（7.16–7.22.1）、git-xing/md-wysiwyg-editor v0.3.2 README/CHANGELOG、epytor 本地代码与临时 jsdom 复现测试
- 两个贡献者 issue 待处理：#16 大文档卡顿（fork 分支已实现）、#15 Clean Markdown 序列化（fork 分支已实现）

---

## 一、列表编辑体验（结论：无需修复，已回退）

### 1.1 有序列表「删除中间一行后编号不连续」→ 撤回

**调查过程**：

- jsdom 复现发现：模拟 keymap 链触发行首 Backspace 时，epytor `listLiftPlugin` 的 `liftListItem` 会让 ProseMirror 分裂列表（实测标签 `1. 2. 1.`），上游 `syncListOrderPlugin` 本身工作正常（程序化删除后编号正确重排）。
- 曾据此开发修复（空项放行默认删除 + lift 后接回分裂列表，含回归测试）。
- **用户实测结论（2026-09-04）**：官方三种列表（有序/无序/任务列表）在真实编辑器中体验正常，**不需要自定义处理**。分析差异：jsdom 测试模拟的是 keymap 链路径，真实浏览器中删除走 prosemirror-view 的 beforeinput 管道，实际未构成体验问题。

**决定**：撤回全部列表修复改动（已 `git checkout` 还原），不引入自定义列表插件。上游限制表（#2413/#2414/#2415）继续按原规则跟踪。

### 1.2 上游列表限制跟踪（不在本批修复）

- 上游 Milkdown#2413（行内样式尾部退出）、#2414（表格单击选中）、#2415（非十进制编号）仍全部 open，`cellClickFixPlugin` workaround 继续保留；升级依赖时按 AGENTS 上游限制表逐项验证。

---

## 二、遗留问题修复（贡献者已实现，本批 review 合并）

### 2.1 Issue #16 大文档输入卡顿（bug）——贡献分支 review 结论：**建议采纳**

- 根因已实证：`webview/editor.ts` `applyMinimalChanges` 每次保存对全文执行 O(n×m) LCS（旧版），阻塞 WebView 主线程。
- 贡献者 fork：`Jurhoo/epytor` 分支 `codex/fix-large-markdown-lag`（净改动 1 个 commit 9bf703d，+266/-104）。
- **实现质量 review**：
  - ✅ 提取独立模块 `webview/utils/minimalDiff.ts`（194 行），归一化函数完整搬移，行为与原版一致
  - ✅ 算法：唯一锚点分段 + 分段有界 LCS（`MAX_LCS_CELLS = 40_000`），无法安全匹配时**快速降级**为全量新输出（诚实退化）
  - ✅ IME 优化落实：composition 期间只暂存 pendingMd 不计算；compositionend 后一次性计算
  - ✅ 计算时机：从「每次 markdownUpdated」移到「防抖提交前」，降低频率
  - ✅ 测试 6 例：单行编辑/插入/重复行/表格风格保留/退化降级/1 万行性能断言（<1s）
- 合并前动作：rebase 到当前 dev（分支基于 main 的 9bcd0fe）；verify + 手测（大文档 + 中文 IME）。

### 2.2 Issue #15 Clean Markdown 序列化（feature）——贡献分支 review 结论：**建议采纳，但有两个决策点**

- 贡献者 fork：`codex/clean-markdown`（净改动 4 commits，+694/-4，14 文件）。
- **实现质量 review**：
  - ✅ 正路集成：官方 `remarkStringifyOptionsCtx` 扩展点 hook text handler，不 fork serializer
  - ✅ `compatible` 模式完整保留旧行为；clean 失败时 try/catch 回退 compatible
  - ✅ 配置链路完整（package.json → Provider → `window.__i18n` → 热更新消息）；`global.d.ts`/i18n 类型已同步
  - ✅ 表格 `<br />` 清理做了 fence 感知与转义管道处理；`preserveTableBreakStyle` 保留源文件拼写风格
  - ✅ spec（241 行）+ 测试（136 行）+ 双语文档齐全
- **决策点 1（产品）**：配置默认值为 `clean`——会改变所有存量用户的保存行为。建议默认 `compatible`（opt-in），或由用户拍板默认值。
- **决策点 2（正确性验证）**：`cleanTextHandler` 覆盖 `encode: []` 禁用了上游字符引用编码，`&`/`<`/`>` 等字符的输出行为变化未被贡献者测试覆盖；合并前需补测验证。
- 合并前动作：rebase 到 dev；补编码场景测试；verify + 手测。

### 2.3 技术债（可选并入本批）

- 🔴 `resolveCustomEditor` 拆分（`MarkdownEditorProvider.ts:183`，322 行）
- 🔴 顶栏 button tooltip 注入正路化（MutationObserver → 官方方案）
- 优先级低于 2.1/2.2，视批次容量决定。

---

## 三、上游 Milkdown 能力吸收

### 3.1 依赖升级：7.22.0 → 7.22.1（本批做）

- patch 版：行内代码 mark 非 inclusive（#2451）+ 行内代码输入规则（#2445）+ readonly 代码块同步（#2455）+ dompurify 3.4.13 安全升级（#2448）。
- 低风险；升级后按上游限制表逐项验证（尤其 #1 行内样式尾部退出是否有改善）。
- 注意：远程有 `dependabot/npm_and_yarn/milkdown/kit-7.22.1` 残留分支，与 AGENTS「dependabot 关闭、统一手动」口径核对后清理或忽略。

### 3.2 未利用能力评估

| 能力 | 说明 | 决策 |
| :--- | :--- | :--- |
| Crepe `blockEdit`（block handle 拖拽移动块） | 7.x 原生 feature | 候选启用，评估与自研组件/样式的冲突 |
| Crepe `placeholder`（空文档占位提示） | 低风险 | 候选启用 |
| `--crepe-base-font-size` 主题变量（7.22.0 #2432） | 简化字号定制 | 与字号设置项一并评估 |
| toolbar shortcuts 派生自 keymaps（7.22.0 #2437） | 无障碍/快捷键一致性 | 观察，无主动动作 |
| AI / diff / streaming（7.21.0） | epytor 已决策移除 Claude 集成 | 不启用 |
| 代码块复制按钮（7.15.0 #1944） | epytor 自研已含 | 无需 |
| lazy CodeMirror（7.21.0 #2313）、XSS sanitize（7.21.3） | 已随 7.22.0 生效 | 无需 |
| 7.22.0 per-keystroke 性能优化（#2436） | 已随 7.22.0 生效 | 无需 |

### 3.3 上游未发布 commits 跟踪

- `#2463 keep user-authored inline br when loading markdown`（利好 #15 的 `<br />` 噪音）
- `#2465 block container types extensible via ctx`
- 下一个 minor 发布后评估是否升级。

---

## 四、友商借鉴（git-xing/md-wysiwyg-editor v0.3.2）

对照后，epytor 缺失且值得采纳的功能：

| 功能 | epytor 现状 | 决策 | 备注 |
| :--- | :--- | :--- | :--- |
| 表格网格选择器（hover 表格按钮拖选 行×列） | 无 | **采纳** | 插入表格体验 |
| FindBar 正则搜索 | 仅大小写切换 | **采纳** | 在其拖拽/大小写基础上加 regex toggle |
| 标题吸顶 + 折叠 | 无 | **采纳** | roadmap 已有；注意其 0.2.1/0.2.2/0.3.2 的 sticky 布局修复经验 |
| 表格换行模式 `tableWrap` 三档 | 无 | **采纳** | roadmap 已有；参考其 normal/aggressive/none CSS 方案，与 epytor「Shift+Enter 软换行」设想比对后二选一或结合 |
| 工具栏溢出菜单 | container query + flex-wrap（有布局跳动问题） | **采纳替换** | roadmap 已有；另注意本地远程分支 `upstream/feature-toolbar-overflow-refactor` 已有部分实现 |
| Frontmatter 可编辑面板 | 仅只读表格展示（`webview/index.ts` L281–309） | **采纳增强** | 参考其 0.2.0 内联编辑 + 0.3.2 列表字段/嵌套结构 |
| 文字对齐（左/中/右） | 工具栏占位按钮未实现 | **采纳** | roadmap 已有 |
| Mermaid 缩放/平移/lightbox | 仅渲染 + 深浅主题 | **采纳** | 参考其 zoom/pan/fullscreen lightbox |
| Tab 键优化（列表缩进/代码块 4 空格/文本 2 空格） | 待确认 Crepe 默认行为 | **候选** | 与 1.2 列表体验同一批验证 |
| 滚动位置持久化 | 已有（setWebviewState scrollY） | 无需 | — |
| 代码块复制 / 语言选择 / 全屏 | 已有 | 无需 | — |
| 外部文件变更 fs.watch | 已有（MarkdownEditorProvider L482） | 无需 | — |
| IME 组合防抖自动保存 | 已有（editor.ts isComposing） | 无需 | — |
| 自定义颜色主题 / 自定义主题 | — | 不采纳 | roadmap 设计决策：坚持 `--vscode-*` 变量方案 |
| Claude 集成 | 已移除 | 不采纳 | 设计决策不变 |
| E2E 测试框架（Playwright Electron） | 无 | **采纳（批次三）** | 呼应 tech-debt「集成测试未搭建」 |

---

## 四·五、官方能力替代与代码减肥（本批核心目标之一）

> 2026-09-04 全仓盘点结论。原则：官方已做得好的，删自定义实现；官方有扩展点的，走扩展点。

### 5.1 死代码（直接删除，零风险）

| 模块 | 体量 | 证据 |
| :--- | :--- | :--- |
| `webview/components/selectionToolbar/`（index.ts + css） | **908 行** | 全仓库无调用方（`setupSelectionToolbar` 等 6 个 export 均无引用）；官方 `feature/toolbar` 启用后遗留 |
| `webview/headingIds.ts` | 18 行 | 空实现（"不操作 DOM，仅保留签名"），连同 `initHeadingIds` 调用一并删除 |

### 5.2 官方能力替代（简化自定义代码）

| 自定义实现 | 官方对应能力 | 处理 |
| :--- | :--- | :--- |
| 代码块复制按钮 tooltip/反馈（index.ts L548–562） | code-mirror feature 原生复制按钮 + `copyText`/`copyIcon`/`onCopy` config（7.15.0 #1944） | 官方按钮已存在，自定义部分仅 tooltip/✔ 反馈 → 简化或保留（低优先） |
| CodeMirror 主题 MutationObserver 补配（editor.ts L548–551） | code-mirror feature `theme` config（静态 Compartment） | tech-debt 已有条目：改初始化传入（低优先） |
| topBar tooltip 注入 + 19 条翻译映射（index.ts L656–696，MutationObserver） | 7.22.0 toolbar accessible name（#2435）；topBar item 是否支持 label/title 待查证 | 若官方 addItem 支持文案字段则删除注入逻辑（中优先，查证后实施） |
| imageView 自研（590 行 + 301 CSS） | 官方 image-block 组件（caption/proxy/maxWidth/onImageLoadError） | epytor 特色（重命名/lightbox/缩放 handle）无法完全替代；列为候选评估项，不在本批 |
| `cellClickFixPlugin`（~130 行） | —（上游 #2414 未修） | 保留，不能移除 |

### 5.3 官方新能力采用（增功能）

见「三、上游 Milkdown 能力吸收」3.2 节：blockEdit、placeholder、`--crepe-base-font-size` 等。

---

## 五、批次划分

| 批次 | 内容 | 目标版本 |
| :--- | :--- | :--- |
| 批次一（本批） | 5.1 死代码删除（selectionToolbar 908 行 + headingIds）+ 2.1 #16 合并 + 2.2 #15 合并 + 3.1 Milkdown 7.22.1 | v1.2.0 |
| 批次二 | 四节功能：网格选择器、正则搜索、吸顶折叠、表格换行、溢出菜单、frontmatter 可编辑、文字对齐、Mermaid 缩放、placeholder/blockEdit 评估 | v1.2.x |
| 批次三 | E2E 测试基建 + 2.3 技术债高优项 + 5.2 官方替代简化项 | v1.2.x / 随批次 |

## 六、通用验收（按 AGENTS 强制流程）

- 每批：`pnpm run verify` 全绿；输出手测清单逐项确认；覆盖率底线（整体 ≥70%）；逐项提交；`git diff --check` 干净。
- 文档联动：README 双份 Settings 表/已知限制、CHANGELOG 双份（发布时）、roadmap 条目状态同步。
- 上游限制表维护：升级后逐项验证 #2413/#2414/#2415，有变化即更新 AGENTS 表与 README 标记。
