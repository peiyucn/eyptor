# 2026-09-08 过度复杂审计第四批 · 手测清单

> 测试包：`releases/epytor-vscode-1.2.0.vsix`
> 覆盖范围：用户实测反馈的两项严重问题（F3）+ 8 项审计简化（P6/P8/E7/E4/E6/F4/P5/P4+C4）
> 使用方法：每项标注 ✅ 通过 / 🛑 有问题（附现象与复现步骤），最后汇总反馈。

## 0. 用户反馈的两个严重问题（本轮根因修复，优先验证）

* [ ] **开着 md 页面时，VS Code 整体输入不再卡顿**——编辑含代码块的 md 文档，中文输入连续打字 30 秒，观察是否还有偶发卡顿（回归：CodeMirror 主题补配观察器 10ms 一次无限回环，持续烧 CPU）
* [ ] **开着 md 页面时，在 DSH 页面切换会话不再带着整个 VS Code 闪动**——保持一个含代码块的 md 标签打开，切到浏览器里的 DSH 页面连续切换会话，观察 VS Code 窗口是否还闪
* [ ] 可选客观验证：命令面板 → `Developer: Open Process Explorer`，观察 epytor webview 进程的 CPU 占用是否稳定在低位（此前会持续占用一个核心的一部分）
* [ ] 主题切换（亮/暗）后代码块高亮跟随，且切换后新插入的代码块高亮也是当前主题

## 1. TOC 折叠状态（P6）

* [ ] 同一文档里放两个同名同级标题（如两处 `## 安装`）→ 折叠第一个，第二个保持展开
* [ ] 折叠状态在关闭标签、重开文档后仍保留（同一标题）
* [ ] 全部折叠 / 全部展开按钮正常

## 2. 图片路径往返（P8 + E7）

* [ ] 图片右键/工具栏「编辑路径」输入相对路径 → 确认后图片正常显示（走 Extension 解析）
* [ ] 图片路径补全（输入 `./` 触发下拉）→ 选中后确认，图片正常显示
* [ ] 图片重命名后（工具栏重命名），再次编辑该图片路径并确认，仍指向正确文件（回归：反向镜像陈旧）
* [ ] **路径含括号/空格的图片**（如 `images/my (v2).png`）：打开 → 编辑一次正文 → 保存 → 用文本编辑器查看源码，src 仍是相对路径（**回归：此前序列化会把括号转义成 `\(`，保存时匹配不上 → webviewUri 泄漏进磁盘文件**）
* [ ] 代码块里写一行裸 `vscode-webview://...` 或含图片语法的文本 → 保存后该代码块内容不被改写（回归 E7）
* [ ] 图片 title（如 `![alt](./a.png "ratio:0.5")`）在编辑路径后仍保留

## 3. 保存链路（E4）

* [ ] Cmd+S 保存后无异常提示，状态栏字数更新
* [ ] frontmatter 面板改一行 → 立即写盘（切到文本编辑器核对源码已更新）
* [ ] 只读文件/保存失败场景：出现「Failed to save file」提示，且标签保留脏点（关窗会提示保存）
* [ ] 切文本编辑器（Cmd+Shift+M）前的内容已落盘（切过去看到的是最新内容）

## 4. 预览/文本双向切换（E6）

* [ ] 文本编辑器 → 预览（Cmd+Shift+M）：**先开后关，无闪动、无双标签残留**
* [ ] 预览 → 文本编辑器（Cmd+Shift+M）：定位到当前视口顶部行号
* [ ] 对未打开预览的 md 执行「切换为预览」命令：直接以文本编辑器打开，不再静默无反应
* [ ] `.markdown` 扩展名文件同样可用快捷键

## 5. 表格换行（P5）

* [ ] 表格单元格内 Shift+Enter 换行 → 保存 → 重开仍换行（源码形态 `<br>`）
* [ ] 空单元格表格：编辑保存后源码里**没有多余的 `<br />` 占位**、列宽不被撑开
* [ ] 含转义竖线 `\|` 的单元格 + 空单元格混合表格往返正常
* [ ] 整行空单元格的表格往返正常
* [ ] 表格内换行与 title/链接等其他语法混排正常

## 6. 路径补全（P4/C4）

* [ ] 正文行内代码里输入 `./` → 下拉出现 → 方向键选择 → Enter 插入路径（正文路径链接）
* [ ] 图片路径输入框输入 `./` → 下拉出现 → 选中插入
* [ ] 目录补全后继续补全（选中目录后自动重新触发）
* [ ] 快速连续输入（A 请求后立刻 B 请求）：下拉显示的是最后一次的结果
* [ ] 下拉打开时点空白处/Esc 关闭

## 7. 用户交互跟踪（F4）

* [ ] 打开大文档（≥1000 行）后立刻用滚轮滚动：延迟定位不再把页面拽回标题行
* [ ] 打开文档后立即点击/输入：不出现假脏标记（标签不出现 ● 圆点）
* [ ] 从文本编辑器切回预览的「定位到光标行」在用户未操作时正常生效
