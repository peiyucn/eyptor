# 2026-09-08 过度复杂审计第四批 · 手测清单

> 测试包：`releases/epytor-vscode-1.2.0.vsix`
> 覆盖范围：用户实测反馈的两项严重问题（F3）+ 10 项审计简化/修复（P6/P8/E7/E4/E6/F4/P5/P4+C4/P1/B1）
> 使用方法：每项标注 ✅ 通过 / 🛑 有问题（附现象与复现步骤），最后汇总反馈。

## 0. 用户反馈的两个严重问题（本轮根因修复，优先验证）

* [x] **开着 md 页面时，VS Code 整体输入不再卡顿**——编辑含代码块的 md 文档，中文输入连续打字 30 秒，观察是否还有偶发卡顿（回归：CodeMirror 主题补配观察器 10ms 一次无限回环，持续烧 CPU）

* [x] **开着 md 页面时，在 DSH 页面切换会话不再带着整个 VS Code 闪动**——保持一个含代码块的 md 标签打开，切到浏览器里的 DSH 页面连续切换会话，观察 VS Code 窗口是否还闪

* [x] 可选客观验证：命令面板 → `Developer: Open Process Explorer`，观察 epytor webview 进程的 CPU 占用是否稳定在低位（此前会持续占用一个核心的一部分）

* [ ] 主题切换（亮/暗）后代码块高亮跟随，且切换后新插入的代码块高亮也是当前主题

  * [ ] 代码块变成黑白的了，无论亮暗主题。

## 1. TOC 折叠状态（P6）

* [x] 同一文档里放两个同名同级标题（如两处 `## 安装`）→ 折叠第一个，第二个保持展开

* [ ] 折叠状态在关闭标签、重开文档后仍保留（同一标题）

  * [ ] 关闭后不用保留

* [x] 全部折叠 / 全部展开按钮正常

## 2. 图片路径往返（P8 + E7）

* [x] 图片右键/工具栏「编辑路径」输入相对路径 → 确认后图片正常显示（走 Extension 解析）

* [ ] 图片路径补全（输入 `./` 触发下拉）→ 选中后确认，图片正常显示

  * [ ] icon改名为icon（1），显示icon%EF%BC%881%EF%BC%89

* [x] 图片重命名后（工具栏重命名），再次编辑该图片路径并确认，仍指向正确文件（回归：反向镜像陈旧）

* [ ] **路径含括号/空格的图片**（如 `images/my (v2).png`）：打开 → 编辑一次正文 → 保存 → 用文本编辑器查看源码，src 仍是相对路径（**回归：此前序列化会把括号转义成 `\(`，保存时匹配不上 → webviewUri 泄漏进磁盘文件**）

  * [ ] 功能正常，但中文括号转移成一串字符了，正常吗？

* [x] 代码块里写一行裸 `vscode-webview://...` 或含图片语法的文本 → 保存后该代码块内容不被改写（回归 E7）

* [ ] 图片 title（如 `![alt](./a.png "ratio:0.5")`）在编辑路径后仍保留

  * [ ] 懒得测了，你在 `@/epytor/test.md`里搞一下，我再测试。

## 3. 保存链路（E4）

* [x] Cmd+S 保存后无异常提示，状态栏字数更新

* [x] frontmatter 面板改一行 → 立即写盘（切到文本编辑器核对源码已更新）

* [ ] 只读文件/保存失败场景：出现「Failed to save file」提示，且标签保留脏点（关窗会提示保存）

  * [ ] 还是不知道怎么切到只读模式

* [x] 切文本编辑器（Cmd+Shift+M）前的内容已落盘（切过去看到的是最新内容）

## 4. 预览/文本双向切换（E6）

* [ ] 文本编辑器 → 预览（Cmd+Shift+M）：**先开后关，无闪动、无双标签残留**

  * [ ] 切到源码不闪，切回预览会闪一下，仔细观察了一下，当前页面标签右侧会闪出一个相同名称的标签页，然后瞬间消失。同时当前tab页会跑到所有tab页最后一个。

* [x] 预览 → 文本编辑器（Cmd+Shift+M）：定位到当前视口顶部行号

* [x] 对未打开预览的 md 执行「切换为预览」命令：直接以文本编辑器打开，不再静默无反应

* [x] `.markdown` 扩展名文件同样可用快捷键

## 5. 表格换行（P5）

* [x] 表格单元格内 Shift+Enter 换行 → 保存 → 重开仍换行（源码形态 `<br>`）
* [x] 空单元格表格：编辑保存后源码里**没有多余的 `<br>` 占位**、列宽不被撑开
* [x] 含转义竖线 `\|` 的单元格 + 空单元格混合表格往返正常
* [x] 整行空单元格的表格往返正常
* [x] 表格内换行与 title/链接等其他语法混排正常
* [ ] 表格内元素的四边内边距不同

## 6. 路径补全（P4/C4）

* [x] 正文行内代码里输入 `./` → 下拉出现 → 方向键选择 → Enter 插入路径（正文路径链接）
* [x] 图片路径输入框输入 `./` → 下拉出现 → 选中插入
* [x] 目录补全后继续补全（选中目录后自动重新触发）
* [x] 快速连续输入（A 请求后立刻 B 请求）：下拉显示的是最后一次的结果
* [x] 下拉打开时点空白处/Esc 关闭

## 7. 用户交互跟踪（F4）

* [x] 打开大文档（≥1000 行）后立刻用滚轮滚动：延迟定位不再把页面拽回标题行

  * [x] 现在的问题是，点开文档，会自动定位到第一个H，而不是frontmatter部分，frontmatter会直接跳过去

* [x] 打开文档后立即点击/输入：不出现假脏标记（标签不出现 ● 圆点）

* [ ] 从文本编辑器切回预览的「定位到光标行」在用户未操作时正常生效

  * [ ] 光标会自动切回第一行第一列

## 8. 标题子系统（P1）

* [x] **状态栏字数实时更新**：在普通段落里连续输入中文，停 1 秒后状态栏「字数/字符数」跟着变（回归：此前标题结构不变就永远不更新，只有改标题或重开文档才刷新）

  * [ ] 停1秒，vscode我开了自动保存，保存后会变。

* [x] 标题折叠箭头：hover 标题出现箭头、点击折叠/展开、折叠后 Enter 自动展开并进入内容

* [x] 吸顶条：滚动时显示当前章节标题（含 H 级别与折叠按钮），点击跳回该标题且不被遮挡

* [x] 嵌套标题（引用块内的 `> ## 标题`）：不出现折叠箭头、不吸顶（与折叠能力一致）

* [x] **嵌套标题改动刷新 TOC**：在引用块内的标题上加字，停 1 秒后 TOC 面板里该条目文字同步更新（回归：此前只改嵌套标题时 TOC 不刷新）

  * [ ] 看起来也是自动保存触发的刷新，一会需要讨论下，无论是状态栏字数/字符数还是这个，是不是应该实时刷新？

* [ ] TOC 点击跳转、折叠/展开全部按钮、同名同级标题各自独立折叠

  * [ ] 能跳转到对应章节，但再往前一个章节的吸顶存在，不符合预期，我理解应该是点击的章节直接顶到头。

## 9. KaTeX 单一版本（B1）

* [x] 数学公式：`$x^2$` 行内、`$$...$$` 块级渲染正常（含分数、上下标）

* [ ] **mermaid 图内数学标签**：写一个含 `$$x^2$$` 的 mermaid 节点/标签，渲染正常（回归：katex 从 0.16.47 收敛到 0.18.1）

  * [ ] mermaid报错

* [x] 亮/暗主题切换后公式与 mermaid 图样式正常

## 10.其他新问题

* [ ] 现在行距默认状态和自行回车明显不一样，回车后的行距比你添加的每行之间行距明显小
* [ ] 任务列表，空内容时，退格键删除任务标记框，能看出来光标轻微上移，其他列表一样。小问题，但让整体质感下降
* [ ] 和非markdown tab页切换，还是会闪。

## 11. 开发者回复（2026-09-08 第二轮，已提交 `a653508`）

> 已修（新 VSIX：`releases/epytor-vscode-1.2.0.vsix`，249 文件 / 2.84MB）：

* **切回预览闪重复 tab**（第 4 节）：改回「先关文本 tab 再开预览」。E6 的「先开后关」是针对资源管理器点开文本 tab 的自动转换路径（那里先关会让 VS Code 先激活上一个文档）；本命令是你显式切换当前文档，先关不会引起跨文档激活转移。→ 请复测。

  * 还是会闪

* **打开文档跳过 frontmatter**（第 7 节）：删除「恢复上次滚动位置」，改为打开后在前几帧内多次复位到顶部（焦点获取 / 布局稳定 / 滚动锚定都会在首帧后把视口推向第一个标题），frontmatter 面板加 `overflow-anchor: none`；你一交互就停止复位。→ 请复测。

  * 目前目测正常，但你这个改法……我理解不是默认就是第一行么，为啥还得多次复位？

* **TOC 跳转后上一个章节吸顶残留**（第 8 节）：跳转目标改为「标题底边落在顶栏底边」——吸顶算法取「最后一个完全滚出顶栏的标题」，这样吸顶条显示的就是你点的章节。→ 请复测（标题本身会被吸顶条代替，若你希望标题仍可见再说）。

  * 应该就是正常的显示到对应章节，没有吸顶标题，现在行为还是不正常，效果比之前更差了

* **表格单元格四边内边距不同**（第 5 节）：官方是 `4px 16px`，改为四周一致 `8px`。

  * 没变化，还是不一致

* **字数刷新时机**（第 8 节）：改为与脏标记同一 300ms 节奏（此前与 TOC 共用 800ms 且被标题签名早退跳过）。TOC 仍是 800ms（只在标题结构变化时重建）。

  * 过了

> 需要你确认/补充信息的：

* **代码块黑白**（第 0 节）：`test.md` 原来只有一个 \`\`\`Text 块——`Text` 语言按设计**无高亮**（纯文本），黑白是正常的。已在 `test.md` 6.0 补了 js/python/json 三个块。→ 请确认这三个块有没有颜色；若也没有，把文档名和块语言发我（jsdom 无法验证真实渲染，只能靠这条信息定位）。

  * 还是黑白的，没有彩色

* **mermaid 报错**（第 9 节）：`test.md` 里原来写的是**单独一行** `$$x^2$$`——那不是合法 mermaid 语句，解析必然报错（与 katex 版本无关）。已改成写在节点标签内（6.2）。→ 请复测；若仍报错请把错误文字发我。

  * 过了

* **中文括号「转义成一串字符」**（第 2 节）：我用真实管线测过 `my（v2）.png`、`my%EF%BC%88v2%EF%BC%89.png`、`my (v2).png`、`图片.png` 四种形态，保存后都还原成原始中文/括号路径，没复现。→ 请把那行源码原文（或截图）发我。

  * 编辑为icon（1），显示icon%EF%BC%881%EF%BC%89

* **折叠状态跨关闭保留**（第 1 节）：这条是我清单写错了预期——webview 状态在标签关闭时被 VS Code 销毁，同一会话内重开不会保留。若你希望「关掉再打开仍记住折叠」，需要把折叠状态存到扩展侧（按文件路径），属新功能，要做我就做。

  * 可以不做

* **md ↔ 非 markdown tab 切换仍闪**（第 10 节）：请区分是「整个 VS Code 窗口闪」还是「md 内容跳动 / 滚动位置变化」——两者根因完全不同，我需要这条信息才能定位。

  * 从非markdown tab切到markdown，markdown页面闪，但很快。比源码/预览切换的闪动小很多

* **行距与任务列表光标上移**（第 10 节）：这两条还没动（视觉细节，需要你给期望值：行距想与段落间距一致，还是把段落间距调小？）。

  * 应该一致，不该因为列表/非列表变化有区别。

> 两条「不知道怎么测」的操作步骤：

* **保存失败提示**（第 3 节）：终端执行 `attrib +R test.md`（设为只读）→ 在 epytor 里改一行 → `Ctrl+S` → 应弹出「Failed to save file」且标签保留脏点；测完 `attrib -R test.md`。

  * attrib是啥？

* **未打开预览时的切换命令**（第 4 节）：用**文本编辑器**打开任意 `.md`（不要用 epytor）→ 命令面板执行 `epytor: 切换为文本编辑器` → 预期正常打开文本编辑器（修复前是静默无反应）。

  * 正常，不过之前也没有这个问题啊

## 12. 开发者回复（2026-09-08 第三轮，已提交 `e14d027`）

> 已修（新 VSIX 已重打）：

* **TOC 跳转**（第 8 节「效果比之前更差」）：上一轮把标题顶到顶栏下沿（标题被吸顶条代替）不是你要的。现在改为——目标标题**正常显示**在顶栏下方，同时隐藏吸顶条；抑制解除条件从 400ms 定时改为「你下一次交互」（滚轮/键盘/滚动条拖拽）。→ 请复测。

  * 过了

* **图片重命名后路径显示 `icon%EF%BC%881%EF%BC%89`**（第 2 节）：根因找到——webview 的「URI → 可读路径」映射只在打开/重载时刷新，重命名产生的新 URI 查不到，于是输入框显示编码串。已在 `imageRenamed` 消息里带上新相对路径并同步映射。→ 请复测（重命名 → 编辑路径，输入框应显示 `icon（1）.png`）。

  * 依然显示为icon%EF%BC%881%EF%BC%89

* **单元格内边距没变化**（第 5 节）：上一轮规则被官方运行时注入的样式覆盖了（同特异性下后注入者胜），已加 `!important`。→ 请复测。

  * 问题依旧

* **行距与回车间距不一致**（第 10 节）：段落下边距 0.4em → 1em。行高 1.6 时，段间距 = 上下留白 0.6em + 1em = 1.6em = 正好一个行距；列表项内容也是 `<p>`，列表与段落节奏自动一致。→ 请复测（若觉得整体太空，告诉我改 0.6em）。

  * 看起来一样了，但行距过大。

* **任务列表删除标记时光标上移**（第 10 节）：官方复选框容器固定 32px，标记消失后行高塌陷；已把它的高度对齐行高。→ 请复测。

  * 问题依旧，并且列表内容与列表标记垂直对齐更歪了。

* **切 tab 回 md 页面闪**（第 10 节）：焦点恢复时的滚动保护只同步比对了一次，浏览器的「滚动到选区」可能晚一帧，已补下一帧校正。→ 请复测。

  * 还是会闪，而且发现非tab页，切md，再切另一个md，闪动明显。两个md之间来回切不闪。

> 还需要你配合的两件事：

* **代码块黑白（最重要）**：请在 md 标签页里按 `Ctrl+Shift+P` → `Developer: Open Webview Developer Tools`，在 Console 里执行下面两行并把输出发我：

  ```js
  document.querySelector('.cm-editor')?.className
  document.querySelector('.cm-content')?.innerHTML.slice(0, 400)
  ```

  若第二行里没有 `<span class="ͼ…">` 包裹的 token，就是语言没加载；再看 Console 有没有红字报错（chunk 加载失败会有）。这条信息能直接定位，我这边 jsdom 验证不了真实渲染。

  * 你自己想办法，别老让我搞。

* **切回预览仍闪 + 保存失败怎么测**：

  * 「文本 → 预览」的闪动，我判断是 **webview 冷启动**（切到源码时 epytor 面板被销毁，切回来要重建整个 webview），加上 VS Code 没有「原地替换 tab」的 API，所以新建的 tab 必然排在最后。两个选项：(a) 接受现状；(b) 切到源码时不销毁 epytor 面板（保留两个 tab，切回来零闪动，但同一文件会有两个 tab）。你选哪个？

    * 两个都无法接受。vscode自带的源码/预览没有这个问题，而且一点闪动都没有。

  * `attrib` 是 Windows 命令行工具；不想用命令行就：资源管理器里右键 `test.md` → 属性 → 勾选「只读」→ 确定，然后再在 epytor 里改一行按 Ctrl+S。

    * 这个场景很奇怪，不测

> 另外两件小事：

* 你测「含括号路径」时把 `images/icon.png` 改名成了 `images/icon（1）.png`，而 `package.json` 的扩展图标指向 `images/icon.png`，导致 VSIX 打包失败——我已改回原名（`test.md` 里的图片引用也依赖它）。以后测这类场景建议复制一份再改名。

* 「为什么要多次复位」：打开文档后有几个动作会把视口推下去——webview 获取焦点时浏览器会把光标滚入视口、Milkdown 渲染完成后的布局变化、浏览器滚动锚定。它们发生的帧不确定，所以我在 0/50/150/400ms 各复位一次，你一交互就停。这是补偿写法，但比「猜一个延迟」稳。

* 之前的问题还没处理

  * 源码/预览切换，光标总会到1行1列，应该维持在原地。

<br>

最后整体态度：这些问题都必须彻底解决，否则这些体验上的问题会导致我们所有的努力都失败，如果我都受不了这样的体验，用户一样受不了。

## 13. 开发者回复（2026-09-08 第四轮，提交 `bf4a357` / `701204a` / `0f9da43`）

> 先说态度：上一轮我让你去开 DevTools 抓诊断，这是把该我做的事推给你了，不对。这轮我自己搭了真实浏览器环境（headless Edge 直接加载构建产物 `dist/webview.js`，桩化 `acquireVsCodeApi` + `__i18n` + init 消息），所有结论都来自实测，不再要你抓日志。

> 找到的两个根因（都是真 bug）：

* **代码块黑白 —— 修好了（`bf4a357`）**：pnpm 装了两份 `@codemirror/state`（6.6.0 / 6.7.1）。Crepe 代码块的 NodeView 来自 6.7.1，用它建的 `languageConf` Compartment；而 `@codemirror/view`（真正创建 EditorView 的那个）用 6.6.0 —— **跨副本的 Compartment 不被识别，`reconfigure(lang)` 静默失效**，语言永远进不了 CodeMirror state。epytor 自己的主题 Compartment 恰好与 view 同副本，所以主题能切、只有语言失效，问题被掩盖了。
  实测证据：修复前 `.cm-content` 内高亮 span **0** 个、`reconfigure` 调用记录里根本没有语言那次；修复后同一文档 js 块 **21** 个高亮 span，reconfigure 记录出现 LanguageSupport。
  修复：`pnpm-workspace.yaml` 的 overrides 加 `"@codemirror/state": ^6.7.1`（全树单副本）。顺带入口 1021.3kb → 973.2kb。
  渲染截图（真实 Chromium）：`_poc/TEMP/epytor-harness-shot.png`
* **源码/预览来回切光标回到 1 行 1 列 —— 修好了（`701204a`）**：Provider 对**完整内容**（含 frontmatter）算行号映射，frontmatter 占掉了 `lineMap[0]`；而 webview 的 DOM 顶层块只有正文，块索引整体错位一格——「预览 → 文本」取到上一块的行号（首块时正好是第 1 行），「文本 → 预览」的滚动同样落在上一块。新增 `computeDisplayLineMap`（剥掉 frontmatter 后按正文块算行号 + frontmatter 行数偏移），四处行号映射统一改用它，补 3 例测试。
* **源码/预览切换闪动 —— 改成零闪动（`0f9da43`）**：你说得对，VS Code 自带预览不闪是因为它**从不销毁再重建**。两条命令都改成「只激活、不重建」：预览 → 文本不再 `dispose()` 面板（面板随标签隐藏但存活），文本 → 预览不再关闭文本 tab、只 `openWith` 复用已有标签。这样来回切换只是激活标签，零闪动、无重复标签、标签顺序也不变。代价：同一文件会同时存在文本与预览两个标签——这也是 VS Code 自带 Markdown 预览的行为。

> 同一次真实浏览器测量的其余三条（都已是修复后的值）：

* **表格单元格内边距**：实测 `padding: 8px 8px 8px 8px`（四周一致）。上一轮你看到「没变化」，我这次量到的 `5px 16px 5px 10px` 是 **frontmatter 面板 key 列**（`.fm-key` 故意留了 16px 右侧间距），不是编辑器表格。若你指的确实是 frontmatter 面板那一列，告诉我，我把它也改成四周一致。
* **行距**：段落文本视觉间距实测 20.0px，行高 21px（节奏一致，不再出现「回车间距比行距小」）。你觉得整体偏大就把行高从 1.5 降到 1.4（段间距会同步收窄），说一声我改。
* **任务列表**：任务项与普通列表项 li 高均为 39px、标记与文字顶边对齐（无塌陷错位）。

> 仍未解决的一条：

* **非 markdown 标签 → markdown 标签切换仍闪**（md ↔ md 之间不闪，只有从文本标签切过来才闪）：这是 webview iframe 从隐藏到可见时的重排，属于 VS Code 宿主侧行为，我目前没有找到可干预的入口。我会继续查，但不敢先给你「已修」的承诺。

## 14. 开发者回复（2026-09-08 第五轮，提交 `a968603`）

* **「两个标签完全不能接受」——已按官方做法重做（`a968603`）**：我去读了 VS Code 内置 `markdown.togglePreview` 的实现，它执行的是 `reopenActiveEditorWith`——**原地把当前标签的编辑器类型换掉**，既不新建也不关闭标签。上一轮我搞的「保留两个标签、只激活」模型是错的，撤回。
  现在两条命令都改成：

  * 预览 → 文本：落盘后 `reopenActiveEditorWith('default')`，随后把光标放到视口顶部行（该命令不接受定位参数，事后设置 selection + 滚到顶）

  * 文本 → 预览：`reopenActiveEditorWith('epytor.editor')`（定位请求照旧先写入，面板重建后消费并滚动到该行）
    效果：**单标签、原地替换**，无重复标签、标签顺序不变，与内置 Markdown 预览一致；旧版 VS Code 无该命令时自动回退到原实现。
    → 请复测：切过去切回来是否只剩一个标签、是否还有闪动。

    * 文本-预览，定位位置错误。另外我试了下官方的切换，官方的预览是只读预览，没有光标，但可以精确互相定位到滑动位置的。你可以看看它的实现方法。
    * 另外，现在源码/预览切换终于不闪了！不错！

## 15. 开发者回复（2026-09-08 第六轮，提交 `a0cad78`）

* **不闪了 —— 收到，`reopenActiveEditorWith` 就是正解**。

* **文本 → 预览定位错误 —— 找到两个原因并修复（`a0cad78`）**，真实浏览器实测复现（8 个目标行，修复前 4/8 落在错误的块）：

  1. `switchToPreview` 取的是**光标行**（`selection.active.line`）。你在文本编辑器里滚动过、光标不在视口内时，预览就跳到完全不同的位置。改为**视口顶部行**（`visibleRanges[0].start.line`）——这正是内置预览的滚动同步口径。
  2. webview 侧 `scrollToSourceLine` 做了「段内比例插值」（按目标行在块内的行占比算滚动偏移）。长段落/长代码块会**滚过目标块**（占比接近 1 时目标块被推出视口顶部），而且来回切换不可逆。改为**元素级定位**（块顶对齐顶栏），与内置预览一致——它的 `data-line` 也只标到块级。
     实测：修复后 8/8 精确落在期望块。

* **顺带**：新增「切回文本时若视口仍停在导航到的那个块，回原始行而不是块首」——否则长代码块来回切换会逐次漂到块首。

* **关于「官方预览没有光标」**：内置预览是只读预览，所以只需同步滚动；epytor 是所见即所得**编辑器**，光标是功能本身。现在的定位口径与它对齐（块级、双向），但保留可编辑性。
  → 请复测：文本编辑器滚到中段（让光标移出视口）→ 切预览，看是否落在你看到的同一段；再切回来，看光标是否还在原处附近。

## 16. 开发者回复（2026-09-08 第七轮，提交 `c76cbca` / `1f082ae`）

> 你上一轮的两条都处理了：内置预览的定位方法我读了它的实现并照做（`c76cbca`）；「非 markdown 标签切回 markdown 仍闪」这次不再猜，我搭了真实 VS Code 环境（临时探针 + 脚本化切标签）把根因抓出来了，已修（`1f082ae`）。

### 16.1 内置预览「没有光标也能精确定位」是怎么做的 —— 已照它的方法重写（`c76cbca`）

读了 `markdown-language-features` 的实现：它给**每个渲染块**都标了 `data-line`（代码块还带 `data-end-line`），同步时按锚点插值，不依赖光标：

| 目标行位置 | 定位算法 |
| :--- | :--- |
| 落在某个块内 | `块顶 + 块高 × (行 - 块起始行) / (块结束行 - 块起始行)` |
| 落在两块之间的空行 | 在「上一块底」与「下一块顶」之间按行数比例插值 |
| 之后没有块了 | 块顶 |

反方向（预览 → 文本）用同一套公式反算。也就是说，**它能精确到行，是因为它有「块的结束行」这个信息**——我们之前只传了块起始行，所以只能落到块首。

epytor 现在也照这套做了：

* `lineMap` 旁边新增 `lineEndMap`（每块的结束行，代码块按围栏整体成段），init / revert / lineMapUpdate 三处一起下发；
* `scrollToSourceLine` 改成块内插值 + 块间空白插值；反方向 `getFirstVisibleSourceLine` 同样按占比反算行号；
* 实测（headless Edge 加载真实构建产物，24 章文档 + 12 行代码块插在中段）：目标行 48/53/61（代码块首/中/末行）分别滚到 1172 / 1265 / 1413 px，顶部块都是代码块本身——长代码块内部也能落到行，不再一律贴块首；
* 顺带清掉两个已无调用方的旧函数（`computeLineMap` / `computeDisplayLineMap`），测试改成直接覆盖新的行区间 API。

### 16.2 「非 markdown 标签 → markdown 标签仍闪」—— 找到根因并修好（`1f082ae`）

**这次没有猜宿主行为，而是把探针装进真实 VS Code 跑了一遍**（临时在 webview 里记录每帧视口尺寸 / 文档高度 / layout-shift / 滚动位置，用脚本自动切标签；探针代码已全部删除，不在提交里）。

实测数据（846×677 编辑器，切到文本标签再切回来）：

```
切到文本标签：  视口 846×677 → 300×150
切回 markdown： 视口 300×150 → 846×677
  · 文档高度 3557 → 4202 → 3557（正文按 285px 重新折行再折回来）
  · layout-shift 0.78 + 0.50（视口 78% / 50% 的内容发生了位移）
  · 中间有 2 帧以 300×150 绘制 —— 这就是你看到的「闪一下」
md ↔ md 切换：  视口全程 846×677、零 layout-shift —— 所以 md 之间不闪
```

**根因**：切到文本标签时，宿主把 webview 容器摘出布局，iframe 失去 `width/height:100%` 约束、回落到 Chromium 的 **iframe 默认尺寸 300×150**；切回来时再放回真实尺寸。我们的正文宽度与 `92vw`/`100vh` 全都跟着这个假视口变了一次，于是整个文档按 300px 重排再排回来。它跟「源码/预览切换」是两回事，所以上一轮的修法对它无效。

**修法**：识别「视口恰等于 iframe 默认尺寸」这一折叠信号，把视口单位与正文宽度**冻结成折叠前的实测值**——排版逐像素不变（折叠期间只是被裁切），宿主放回真实尺寸时零重排：

* 新增 `webview/utils/viewportFreeze.ts`：折叠判定 + 冻结状态机（纯函数，15 例单测）；body 宽度基准由 ResizeObserver 维护（纵向滚动条出现/消失会改 body 宽度但不触发 resize，基准陈旧会让正文在切回时横移一个滚动条宽）；
* 视口单位统一改走 `--epytor-vw` / `--epytor-vh` 变量（正文宽高、目录高度、灯箱、图片选择器、tooltip 共 8 处），折叠期整体覆盖；
* 目录自动显隐、顶栏溢出测量、吸顶条重算在折叠期跳过——否则目录会误判「放不下」而收起、顶栏按钮会收进「⋯」再展开。

**修复后同一环境实测**：layout-shift **0**（不再有 shift 条目），文档高度全程 3557（没有任何一帧重排），滚动位置 1396 保持不变；md ↔ md 切换行为不变。

→ 请复测：

1. 非 markdown 标签 → markdown 标签，来回切几次：页面是否还闪（预期：不再有整页重排，最多少 2 帧的裁切）。
2. 文本编辑器滚到文档中段 → 切到别的标签 → 切回来：视口位置是否还在原处（预期：不动）。
3. 顺便确认 md ↔ md 切换、源码/预览切换没有被这轮改动影响。

## 17. 开发者回复（2026-09-08 第八轮，提交 `6139b69`）

> 两条都处理了：切换时机按官方实现重写（碰到即固定），并改成多级 + 源码形式行（你选的方案）。

### 17.1 切换太晚 —— 读了官方实现，改成「碰到即固定」

官方（VS Code 内置编辑器吸顶滚动 `stickyScrollController.findScrollWidgetState`）的判定：

```
第 d 层的吸顶行画在 topbarBottom + (d-1) * 行高
该标题占这一行 ⟺ 标题顶边 < 该行 y ≤ 本章节底边
```

也就是：**标题顶边碰到它那一行就进入固定状态**，本章节末尾划过该行时让位给下一个标题。我们旧实现取的是「最后一个 bottom ≤ 顶栏底」的标题——要等标题**整个**滚出顶栏才切换，正好晚一个标题高度，这就是你感觉到的「新章节都滑出可见范围才换」。

现在 epytor 的几何判定与官方逐字对齐（`webview/utils/headingSticky.ts`，纯函数 + 14 例单测）。

### 17.2 多级 + 源码形式（按你选的方案）

* 最多 **3 级**，且不超过视口高度的 25%（同官方口径）；外层在上、内层在下，每级 22px，3 级共 66px。
* 行内不再渲染标题字号，改为**源码形式**：`##` 前缀（等宽、弱化色）+ 纯文本，12px。
* 每一级都可点击跳回该标题、都可折叠（左侧 chevron）。
* 章节底边 = 下一个「层级 ≤ 自己」的可见标题顶边（折叠隐藏的内容已剔除），嵌套深度按标题层级栈算（跨级 h1 → h3 记为第 2 层）。

### 17.3 实测

* 单测：371 通过（新增/改写 17 例：几何 14 + jsdom 端到端 3，含「碰到即固定」「未碰到不切换」「多级逐级显示」）。
* 真实浏览器（headless Edge + CDP、真实 rAF）：21 个标题的三级文档，373 个滚动采样点（含每个标题切换点 ±30px 密集采样）与期望几何逐点比对，**不匹配 0**。切换瞬间实测（第 0.1 章，其吸顶行 y=58）：

```
目标标题 top=26 → [第 0 部 | 第 0.1 章 | 第 0.1.0 节]
目标标题 top=36 → [第 0 部 | 第 0.1 章]      ← 子级行因章节切换让位
目标标题 top=66 → [第 0 部 | 第 0.0 章]      ← 顶边划过自己的行 y=58 后交还给上一章
```

* 版面实测：bar top=36（顶栏底）、3 行各 22px、marker 依次 `#` / `##` / `###`、行内折叠按钮齐备。

→ 请复测：滚动到章节边界看切换是否「碰到就换」；深文档里看多级是否清楚、占屏是否可接受（3 级 66px）。

## 18. 开发者回复（2026-09-08 第九轮，提交 `6f90093` / `b80eb85`）

> 两条都修了，都是真 bug，实测数据在下面。

### 18.1 吸顶第一级点击没反应（`6f90093`）

根因：行点击处理沿用了旧代码的 `pos > 0` 守卫，而 `pos` 是节点起始位置，**文档第一个标题就是 0**——所以第一级永远被挡掉，二三级正常。改为 `pos >= 0`，并补了 jsdom 回归用例（点击第一级 → `window.scrollTo` 被调用）。

### 18.2 非 md ↔ md 切换仍闪（`b80eb85`）

上一版修掉了「整页重排」，但你复测仍有闪动。我又跑了一次真实 VS Code 逐帧探针（每帧记录视口、文档高度、正文透明度、媒体查询状态），抓到了残余那一帧：

```
raf#913  iw=300 ih=150  frozen=false  op=1  dh=4202  dt=1839   ← 折叠后的第一帧
resize   300×150（比上面那帧晚 7ms 才到）                        ← JS 冻结来不及
raf#914  iw=300 ih=150  frozen=true   op=0  dh=4202
```

**根因**：宿主把面板显示出来后，iframe 先以默认 300×150 绘制一帧（正文按 300px 重排，文档高度 3557 → 4202），而 `resize` 事件 60ms 后才到——上一版的 JS 冻结只能从第二帧起生效，所以你还看到「一帧重排 + 若干帧被裁在左上角的正文」。

**修法（两层）**：

1. **纯 CSS 兜底**——媒体查询在样式/布局/绘制之前求值，所以同一帧就能生效：JS 持续把「上一次真实尺寸」写进 `--epytor-last-*`，折叠尺寸（300×150）下用它们钉住排版并隐藏正文。第一帧就已经是正确版式且不可见。
2. **JS 冻结**（上一版）继续负责非 300×150 的其他折叠尺寸与滚动条抑制；另加 `data-epytor-tiny-real`：用户真把编辑区拖到 300×150 时不隐藏。

**实测（真实 VS Code，文本标签 → 切回 markdown）**：

```
修复前第一帧： dh=4202  op=1   （重排可见）
修复后第一帧： mq=true  op=0  dh=3557  （版式已钉住、正文不可见）
此后各帧：    dh 全程 3557、滚动位置 1396 不变
```

headless Edge 同口径复核：媒体查询翻转到折叠尺寸的那一刻，`--epytor-vw=846px`、body 宽度 831px、`opacity=0`、文档高度不变。

**验证**：`pnpm run verify` 全绿（47 文件 / 376 测试；本轮新增 5 例：吸顶第一级点击 1 + 过渡帧判定 4）。

→ 请复测：

1. 吸顶第一级（最外层那一行）点击是否跳回对应标题；
2. 非 md ↔ md 来回切几次，是否还闪（预期：切过去是「底色 → 内容」，没有重排也没有裁切闪动）；
3. 顺带确认切回来后滚动位置没变。

## 19. 开发者回复（2026-09-08 第十轮，提交 `6da8b00` / `1e61a80`）

> 两条都查到了根因并修了。这轮为了看清「闪」，我用 CDP 给 VS Code 工作台**逐帧录屏**（418 帧）＋ 页面内逐帧探针，把过渡期的每一帧都对齐到了时间线。

### 19.1 切源码后回到 1 行 1 列（`6da8b00`）

探针实测（首次切换、该文件的文本编辑器还没创建过）：

```
switchToTextEditor-msg   line=58
switchToTextEditor-done  sel=57 visTop=0     ← 光标设对了，视口没滚
（第二次切换时编辑器已存在：visTop=52 正常）
```

**根因**：`reopenActiveEditorWith('default')` 之后文本编辑器**首帧还没布局**，紧接着的 `revealRange` 被丢掉——所以视口停在文件开头（第二次切换之所以正常，是因为编辑器已存在）。随后切回预览时，extension 取的是「视口顶部行」= 第 1 行，预览也就跟着回到开头，于是「再切回来也没法看了」。

**修法**：立即定位一次，再按 50/150/350ms 重试；用户已自行移动光标、或目标行已在可见范围内就不再干预。

**实测（同一探针）**：首次切换后 `sel=57`，800ms 时 `visTop=52 visBottom=86`（目标行可见）；第二次切换 `sel=53 visTop=48`；切回预览携带的 pending 行号也随之变成 53 / 49（不再是 1）。

### 19.2 非 md → md 切换仍闪（`1e61a80`）

录屏逐帧比对发现：过渡期有 **2 帧整屏空白**（上一版把过渡期整块隐藏了），观感是「内容 → 空白 → 内容」的闪烁。

**改为过渡期保留正文**：排版仍冻结成折叠前的正确版式（文档高度全程 3557、零重排），只是被视口裁切在左侧；同时继续抑制该期间会出现在编辑区中间的滚动条。

**实测（真实 VS Code，非 md 标签 → md 标签）**：

```
+43ms 300×150 op=1 dh=3557   ← 正文可见（不再空白）
+46ms 300×150 op=1 dh=3557
+65ms 846×677 op=1 dh=3557
```

即正文全程可见、无空白帧、无重排；过渡只剩约 2 帧的左侧裁切（这 2 帧是宿主把 iframe 从默认 300×150 放回真实尺寸的时间窗，扩展侧改不了）。

> 补充说明这个时间窗的来源：宿主显示面板后，iframe 会先以 Chromium 默认尺寸 300×150 布局约 60–100ms，resize 事件才到。所以那 2 帧里我们能做的只有「不重排、不闪白、不跳动」。

→ 请复测：

1. 切源码：视口是否停在你刚才看的那一段（不再回文件开头），再切回预览是否也在同一段；
2. 非 md 标签 ↔ md 标签来回切：是否还有整屏空白（预期没有；若仍觉得闪，麻烦说一句是「整块变空」还是「内容位置跳一下」，两者根因完全不同）。

## 20. 开发者回复（2026-09-08 第十一轮，提交 `fdbd4d4`）

> 你问得对——上一版**不是**官方的做法，是绕路。我去把内置 `markdown-language-features` 的定位实现完整读了一遍，按官方口径重写了（`fdbd4d4`），定时重试已删除。

### 20.1 官方怎么做的

* **记住位置**：预览侧把它当前的行号按 URI 存起来（`setPreviousStaticEditorLine`）；文本侧用 `onDidChangeTextEditorVisibleRanges` 记录**视口顶部行**（还带行内字符比例，做到亚行精度）。
* **切回文本时定位**：在 **`onDidChangeActiveTextEditor`** 里取出刚才存的行号，执行 `revealRange(range, AtTop)`。
* 关键点：**没有定时器、没有重试**——因为事件触发时编辑器已经建好、可以正常滚动。

### 20.2 上一版为什么是绕路

我们当时是在 `reopenActiveEditorWith` 命令**返回后立刻** reveal，那时文本编辑器首帧还没布局，reveal 被丢弃（所以视口停在文件开头），于是我用「50/150/350ms 重试」去补——能用，但属于拿定时器兜住时序问题，不是官方路径。

### 20.3 现在改成官方口径（`fdbd4d4`）

* 新增 `_pendingEditorReveal`（URI + 行号），在构造函数里注册 `onDidChangeActiveTextEditor`，**文本编辑器真正 active 时**才 `revealRange(..., AtTop)`（与官方 `vv()` / `Jle()` 同口径）。
* `switchToTextEditor` 只登记待恢复行；命令返回后若事件没触发（旧版 VS Code / 已经是同一个文本编辑器）补一次。
* 删除 `TEXT_EDITOR_REVEAL_RETRY_MS` 与重试循环。

**实测（真实 VS Code 探针）**：webview 滚到 line 60（`sy=1396`、视口顶部行 58）→ 切源码 → `settled line=58 sel=57 visTop=52 visBottom=86`（目标行可见）。纯事件触发与「事件 + 重试」结果完全一致，所以重试确实多余，已删。

> 预览方向仍是官方口径：切回预览时把文本编辑器的**视口顶部行**作为 pending navigation 下发，webview 用锚点插值滚动到该行（`data-line`/`endLine` 那套，第六轮已对齐）。

**验证**：`pnpm run verify` 全绿（47 文件 / 376 测试）。

## 21. 开发者回复（2026-09-08 第十二轮，提交 `f122de8` / `bc528e3`）

> 你回了两个关键信息（入口 = 编辑器标题栏按钮；闪的是编辑区、很快、像「整块变空」），两条都据此重做了。

### 21.1 位置固定无效 → 改成官方的「持续记录 + 激活时恢复」（`f122de8`）

我核对官方 `markdown-language-features`：它**不在切换命令里临时算一次**，而是持续记录两侧视口顶部行（预览侧 `setPreviousStaticEditorLine`、文本侧 `onDidChangeTextEditorVisibleRanges`），再在 `onDidChangeActiveTextEditor` 里恢复。我们此前只在 epytor 自己的命令里算，覆盖面不够（也解释了你为什么觉得「无效」——路径只要不是我们那条就丢位置）。

现在照官方做了：

* webview 滚动防抖后上报**视口顶部源码行**（`viewportLine`），首屏稳定后再上报一次；
* 扩展侧持续记录 `_lastPreviewLines`（预览）与 `_lastTextLines`（文本编辑器）；
* `onDidChangeActiveTextEditor` 时：优先用「切到源码」登记的精确行，否则用预览上报的行恢复；
* 面板激活时定位优先级：显式导航 > 全局兜底 > 文本编辑器视口顶部行。

**实测（真实 VS Code，故意走内置路径 `vscode.openWith`，不经我们的命令）**：

```
预览滚到 line 60 → viewportLine line=59
切文本：activeTextEditor previewLine=59 sel=58 visTop=53（目标行可见）
切回预览：preview-scrollToLine line=54 → webview viewportLine line=54（落位正确）
```

### 21.2 编辑区整块变空 → 过渡期关掉模糊（`bc528e3`）

探针逐帧数据显示：过渡期**DOM 侧正文始终可见**（`op=1`）、文档高度全程不变——所以空白不是排版造成的，而是合成器在宿主重新挂载 webview（把容器 `appendChild` 到新 pane）后重建图层、必须重新光栅化；而 `backdrop-filter`（顶栏/目录的 blur）要求先捕获并模糊背景，首帧因此被推迟 → 用户看到编辑区空白 2 帧。

**改法**：过渡期（折叠/回落 300×150 的那几十毫秒）关掉顶栏、目录面板、目录拖拽条的模糊——此刻它们后面就是正文与底色，有无模糊的视觉差异可忽略，但首帧能立刻画出来；同时去掉冻结期的 `overflow-x: hidden`（滚动条抑制已由 `scrollbar-width: none` 完成，少一次根元素重排）。

**验证**：`pnpm run verify` 全绿（47 文件 / 376 测试）；headless Edge 复核媒体查询命中时排版仍钉住（`--epytor-vw=846px`、body 宽度 831、文档高度不变、正文 `opacity=1`）。

> 说明一下这个「空白」的物理边界：宿主显示面板后会重新挂载 webview 容器，iframe 短暂回落 Chromium 默认尺寸 300×150（实测 15–72ms）再放回真实尺寸。这段时间里扩展侧能做的是「不重排、不闪白、不跳位、把首帧画出来」；剩下的就是合成器重新光栅化本身。

→ 请复测：① 切源码/预览，位置是否保持（也试试标签右键「重新打开方式」）；② 编辑区是否还会整块变空。

## 22. 开发者回复（2026-09-09 第十三轮，提交 `e74d766` / `86bbc1b` / `ba84e7e`）

> 你复测的两条（① 还是会闪、无明显改善；② 源码→预览有定位但不精确、预览→源码完全无法定位）这轮我没有再猜——把「闪」和「定位」各自放进可控探针里量化，两个都是真 bug，也都能给出修复前/后的对照数据。

### 22.1 闪动的根因：那条媒体查询从来没生效过（`e74d766`）

折叠第一帧的纯 CSS 兜底写在 `@media (width: 300px) and (height: 150px)` 里，但选择器用的是 `html`（类型选择器，特异性 0,0,1），而基础变量声明在 `:root`（伪类，0,1,0）——**`:root` 胜出，媒体查询里的变量赋值一次都没生效**，所以折叠第一帧仍按 300px 重排（JS 侧冻结要等 `resize` 事件，实测 60–100ms 后才到，来不及救第一帧）。

隔离实测（headless Edge，300×150 视口，只加载 CSS、不加载任何 JS）：

| CSS | `--epytor-vw` | 正文容器宽 | 标题（h1）尺寸 | 文档高 |
| :--- | :--- | :--- | :--- | :--- |
| 修复前（`html`） | `100vw` | 276px | 180 × **179**px（折成 4 行） | 764 |
| 修复后（`:root`） | `846px` | 778px | **682** × 45px（一行，被视口裁切） | 367 |

也就是说：折叠那两帧里整个文档按 276px 重排了一次（标题断成 4 行、正文全部重新折行），再排回来——正是你看到的「闪出一排字」。修复后排版保持折叠前的 778px 宽，只是被 300px 视口裁切，不再重排。

> 物理边界仍然存在：宿主把 webview 摘出布局后 iframe 会回落 300×150（实测 15–72ms），这段时间里扩展侧能保证的是「不重排、不跳位、滚动位置不变」；那 300×150 的可见范围只能是被裁切的一块。

### 22.2 定位失效的根因：行号表和编辑器的块结构对不上（`86bbc1b` / `ba84e7e`）

用你那份清单（554 行）实测：

```
编辑器顶层块（DOM 内容块）  142 个
行号表（旧实现，按空行分段） 232 项   ← 错位最多 90 块
```

两个原因：

1. **空行分段 ≠ 编辑器块结构**：列表项之间有空行时，旧实现把每项各算一段，而 CommonMark 把它们解析成**一个** list 块——整份文档越往后错得越多。
2. **DOM 里混着一个 widget**：ProseMirror 把虚拟光标的 widget（`<div contenteditable="false" class="ProseMirror-widget">`）也放进编辑区 DOM，而且它是**第一个**子元素——块索引整体错位一格。更糟的是「滚动前等首块高度」的守卫用的就是 `children[0]`，那是个空 div（高度 0），守卫永远不通过 → **切回预览的定位一次都没执行过**。

**改法**：

* 行号表改用与渲染端同口径的解析（`unified` + `remark-parse` + `remark-gfm` + `remark-math`），取 mdast 顶层节点的真实起止行；链接引用定义、脚注定义、frontmatter 等不产生渲染块的节点剔除。实测块数 142 ↔ 表项 142，逐块对齐。
* webview 侧改用 `doc.forEach` + `nodeDOM` 取**内容块**（天然跳过 widget），正算、反算、就绪判定三处统一。
* 定位时扣掉吸顶条高度，并在下一帧吸顶插件更新后再校正一次（滚动前还没吸顶，实测差一整行 ~45px）。

**实测（真实 VS Code，同一份清单，脚本化滚动后切换）**：

| 场景 | 修复前 | 修复后 |
| :--- | :--- | :--- |
| 预览滚到「6. 路径补全」→ 切源码 | 视口落在第 **31** 行（2. 图片路径往返） | 视口落在第 **82** 行（6. 路径补全） |
| 源码视口顶部第 12 行 → 切回预览 | — | 预览顶部显示同一列表（差 1 个列表项，与官方同款「块内插值」同精度） |

### 22.3 验证与交付

* `pnpm run verify` 全绿：47 文件 / **383 测试**（新增 7 例：空行分隔列表合并、嵌套列表、GFM 表格、多行引用、链接定义剔除、缓存命中、性能）
* 提交：`e74d766`（媒体查询失效）、`86bbc1b`（行号表按渲染块生成）、`ba84e7e`（按文档块取 DOM + 吸顶条高度）
* VSIX 已重打包：`releases/epytor-vscode-1.2.0.vsix`

→ 请复测：① 非 md 标签 ↔ md 标签切换（现在应是「底色 → 内容」，不再有整块重排）；② 预览滚到任意章节切源码、再切回预览，是否停在同一章节。

## 23. 开发者回复（2026-09-09 第十四轮，提交 `fcceec8`）

> 你回「切换定位 ok 了，但非 md 切 md 还是会闪一下，非常明显」。这轮我把**官方内置预览**放进了同一个探针做正面对照——结果是：它和我们的机制根本不同，而差别正是闪动的来源。

### 23.1 为什么官方不闪：它折叠时 iframe 被整个摘出 DOM

同一台机器、同一套探针（真实 VS Code 窗口，逐帧记录 `iframe.webview` 的挂载状态与尺寸）：

| | 折叠时（切到文本标签） | 切回来 |
| :--- | :--- | :--- |
| 内置预览（`markdown.preview`） | `isConnected=0`、尺寸 `0×0` —— **iframe 被摘出 DOM** | 宿主新建一个 iframe，容器已是正确尺寸 |
| epytor | `isConnected=1`、尺寸 **300×150**（保持挂载、被压扁） | 同一个 iframe 被放回 791×738 |

根因：官方预览是 `createWebviewPanel` + `retainContextWhenHidden: false`——隐藏时宿主直接销毁 iframe 内容，**它从来没有以 300×150 画过一帧**；epytor 是自定义编辑器 + `retainContextWhenHidden: true`，iframe 活着、被压成 300×150 之后仍然会绘制，那 2–3 帧里你看到的就是「正文挤在左上角一小块」。

### 23.2 改法：那几十毫秒只画底色（`fcceec8`）

等尺寸的这几十毫秒里画任何内容都是错的（画全宽会被视口裁成一小块、画重排会跳），而「什么都不画、只留底色」与官方重挂载时的表现完全一致（官方那时编辑区同样是底色）。

* 纯 CSS 兜底：折叠尺寸下 `body { visibility: hidden }`——媒体查询在布局/绘制之前求值，第一帧就成立；
* JS 冻结/回落态同样隐藏（`.epytor-viewport-shrunk` / `.epytor-viewport-frozen`）；
* 例外：用户**真**把编辑区缩到 300×150 并稳定停留 `TINY_REAL_SETTLE_MS`（600ms）时打标记 `data-epytor-tiny-real` 不隐藏，并按真实尺寸重新排版（否则小窗口下编辑区会永久空白）；焦点变化时清除标记，保证长时间折叠后切回来第一帧仍然不画内容。

### 23.3 实测

| 指标 | 修复前 | 修复后 |
| :--- | :--- | :--- |
| 切换过程录屏帧数（同一脚本、同一文档） | 80–84 帧 | 25–28 帧 |
| 其中「内容挤在左上角」的帧 | 2–3 帧 | **0 帧** |
| 折叠时 `body` 计算样式（隔离 CSS 测试） | `visibility: visible`、`--epytor-vw: 100vw` | `visibility: hidden`、`--epytor-vw: 846px` |

录屏逐帧确认：过渡帧里编辑区是**纯底色**（工具栏、正文、目录都不画），与内置预览重挂载时一致。

### 23.4 验证与交付

* `pnpm run verify` 全绿：47 文件 / **386 测试**（新增 3 例：真尺寸确认的标记 / 尺寸变化清除 / 阈值内恢复不标记）
* 提交：`fcceec8`
* VSIX 已重打包：`releases/epytor-vscode-1.2.0.vsix`

→ 请复测：非 md 标签 ↔ md 标签来回切几次（现在应该只剩「底色一闪 → 内容」的极短过渡；如果你连这个也还觉得明显，告诉我，我再想别的路子）。

## 24. 开发者回复（2026-09-09 第十五轮，提交 `bb19e83`）

> 你说「依然闪，而且现在变成闪两次了」。这轮我换了测量工具——**60fps 真实窗口录屏**（ffmpeg gdigrab + 脚本化切标签，逐帧核对）——才看清之前的 CDP 探针漏掉了什么。

### 24.1 真相：宿主压扁 webview 的时间是 ~150ms，不是 35ms

之前的 CDP 探针按 rAF 轮询 iframe 元素的尺寸，得到「回落 300×150 只有 35ms」。60fps 录屏逐帧核对后发现：**编辑器区域处于「错误状态」的时间是 130–170ms**（8–10 帧），探针的轮询漏掉了中间态。

于是三种画法在录屏里的实测：

| 折叠期画什么 | 录屏表现 | 帧数 |
| :--- | :--- | :--- |
| 正文（排版钉住、被裁切） | 左上角一块「一排字」（你之前看到的） | 10 帧 |
| 什么都不画（纯媒体查询） | 生硬的「消失 → 出现」 | 4 帧 |
| 什么都不画（JS 类驱动） | 同上，但空白被拖长 | 7 帧 |

顺带查出一个实现问题：`.epytor-viewport-shrunk` 类由 resize 事件驱动，实测比尺寸恢复晚 ~65ms，会把空白期从 4 帧拖到 7 帧——你测到的「闪两次」就是这一版。

### 24.2 改法：压暗 + 淡入（`bb19e83`）

折叠期不画正文（`visibility: hidden`），同时把透明度压到 `0.1`；这段过程 webview 本身不可见，所以压暗用户看不到。尺寸恢复后，`html/body` 上的 90ms `transition` 把透明度从 0.1 平滑升到 1——用户看到的是「底色 → 内容淡入」，既没有碎片，也没有硬切。

实测（60fps 录屏，同一脚本）：压暗期 ~8 帧 → 淡入 3 帧 → 全量内容。

### 24.3 还剩下的物理边界（说实话）

那 130–170ms 是宿主行为：它先把 webview 容器设为 `visibility: visible`，再测量锚点元素、设置尺寸——中间这段时间 iframe 只有 300×150。官方内置预览没有这个问题，是因为它 `retainContextWhenHidden: false`：折叠时 iframe 被整个销毁（实测 `isConnected=0`），重新挂载时容器已是正确尺寸，**它从来没有以 300×150 画过一帧**。

要不要照官方那样做（销毁 + 重建）？代价是每次切回 md 都要重建 Milkdown（这份 554 行文档实测 ~1.4 秒），那 1.4 秒里只能显示一张预渲染快照且不可编辑——我判断比现在这 150ms 更难接受，所以没做。如果你更在意「零闪动」而不是「切回即能编辑」，告诉我，我按那条路做。

→ 请复测：非 md ↔ md 切换是否还看到「一排字」或「两次闪」。



