# v1.2.0 手测清单

> 测试包：`releases/epytor-vscode-1.2.0.vsix`
>
> **状态口径（2026-09-12 收紧）**：`[x]` **只表示 owner 明确确认过**（本清单注明出处），
> 其余一律 `[ ]` 待复测。⚠️ = 已知限制（上游 / 平台，本版不修）；🛑 = owner 反馈未通过。
>
> 为什么收紧：上一版合并清单时把更早轮次的勾选直接继承为"已通过"，而排版、TOC、列表语义
> 此后都改过多轮，旧勾选已失效——**没测过就是没测过**。

## 1. 本轮（2026-09-12）

### 1.1 Word 式多级列表标记（owner 已确认「效果不错」）

* [x] 三级有序列表显示 `1.` / `a)` / `i.`，第四层回到 `1.` —— ✅ 2026-09-12 owner 确认
* [x] 三级圆点列表显示 ● / ■ / ◆，第四层回到 ● —— ✅ 同上
* [ ] 任务列表勾选框不受影响
* [ ] 源码与存盘不变（仍是 `1.` / `-`，不出现 `a)` 这种非法写法）
* ⚠️ 上游：`a)` / `i.` 与 ■ / ◆ 是显示效果，Markdown 语法本身不支持（[Milkdown#2475](https://github.com/Milkdown/milkdown/issues/2475)）

### 1.2 切标签「点一下闪一下」回归修复（owner 已确认「没有了」）

* [x] 点当前标签 / 侧边栏 / 终端，编辑区不再闪 —— ✅ 2026-09-12 owner 确认
* ⚠️ 平台：切回标签时编辑区仍会空白一下（VS Code 对保活 webview 的行为，扩展侧无法干预）

### 1.3 排版尺度（含行距统一的**新**改动，需复测）

* [ ] 全文一档行高 + 一档块间距（10.5px），标题与分隔线取 2× 档
* [ ] 列表：标记与同行文字对齐、项间距与段间距一致、每级缩进 27.3px
* [ ] **列表行距统一**：父子行与同层行同样松紧（本轮新修，未复测）
* [ ] 引用块、表格单元格上下留白相等
* [ ] 源码软换行不再多出一行
* [ ] 标题折叠按钮悬挑在正文列外、与标题留 8px

### 1.4 TOC 两态（本轮重写，未复测）

* [ ] **点关闭后**，窗口从很宽拖到很窄再拖回很宽 → TOC **不再自动出现**
* [ ] **点打开后**，窗口拖窄到不足 → **自动收起**（手动打开不压过宽度规则）
* [ ] 没点过关闭时：窗口 ≥ 阈值自动出现、< 阈值自动收起（阈值 = 编辑页宽度 + 100，默认 1000）
* [ ] 打开态**推正文、不遮字**
* [ ] 把手是方向箭头，方向随状态翻转（收起 › / 展开 ‹）
* [ ] 界面上不再有图钉与整体收展按钮；标题显示 `Contents`（中文「目录」）
* [ ] 滚动时 TOC 高亮跟随当前章节、**不在两节交界处来回跳**

### 1.5 其他本轮改动

* [ ] 列表项行首 Backspace：断开列表、该项变普通行；有序列表后半段**延续编号**
* [ ] 「打开方式…」里本编辑器显示为 `EPYTOR`（一段）
* [ ] 顶部工具栏品牌为 `🦖EPYTOR`

## 2. 待修 / 未通过（owner 反馈，尚未闭环）

* [ ] 🛑 表格整行 / 整列删除工具栏的样式回到官方风格，未适配 epytor 主题
* [ ] 🛑 Frontmatter 点「+」新增行无效
* [ ] 🛑 Mermaid 全屏：工具栏在，但三个按钮都无效
* [ ] 🛑 TOC 标题「Table of Contents」字号过大（标题文案本轮已改为 Contents，字号未动）
* [ ] 🛑 顶栏「⋯」溢出菜单：窗口变窄时与最后一个按钮重叠，菜单右缘被页面边缘压住
* [ ] 🛑 切换文件 / 切回 epytor 标签后编辑焦点丢失
* [ ] ⏳ Mermaid 默认缩放与区间是否合适
* [ ] ⏳ 一万行文档中文输入卡顿（已知限制为约 3000 行内流畅）

## 3. 功能回归（每项都需重新确认——本版对这些模块有改动）

> 更早轮次测过，但此后排版、列表语义、TOC、标记渲染都改过，**不能沿用旧结论**。

* [ ] 打开 / 编辑 / 保存 / 另存；`files.autoSave` 四档跟随
* [ ] 表格：单元格光标、行列插入 / 删除、行列选中工具栏、拖拽 handle
* [ ] 查找栏：正则开关、`Aa`、无效正则提示、大文件高亮封顶
* [ ] 表格网格选择器（8×8）、Esc / 外部点击关闭
* [ ] 标题吸顶（最多三级）+ 同级折叠；折叠状态关标签后仍保留
* [ ] 表格换行两档 + 单元格内 Shift+Enter；`<br />` 往返闭环
* [ ] Frontmatter 面板编辑、含引号冒号 URL 的值往返一致
* [ ] 图片：相对路径往返、路径含空格括号、重命名、上传失败提示
* [ ] 源码 ↔ 预览双向切换保持位置（大文件不丢位置）
* [ ] Clean Markdown 保存模式（`clean` / `compatible`）
* [ ] 大文档（≥1000 行）连续输入不卡顿；中文拼音组合流畅
* ⚠️ 上游 #2413：行内样式尾部无后续内容时无法退出
* ⚠️ 上游 #2414 / #2415：表格单击选中整格暂时关闭；有序列表多层级编号（显示层已 workaround）

## 4. 审计批次（2026-09-08）遗留

> 多为内部重构（死代码删除、路径与状态一致性、用户交互跟踪、KaTeX 单一版本等），未逐项手测；
> 涉及用户可见行为的部分已并入第 3 节。需要时按 `git log a653508..6139b69` 的提交逐项抽测。
