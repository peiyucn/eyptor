# v1.2.0 手测清单

> 测试包：`releases/epytor-vscode-1.2.0.vsix`
> 使用方法：每项标注 ✅ 通过 / 🛑 有问题（附现象），最后汇总反馈。

## 1. 死代码删除回归

* [x] 选中文字 → 官方浮动工具栏正常弹出（粗体/斜体/删除线/链接等）
* [x] 表格操作正常：单元格光标、行列插入/删除按钮（反馈：但无法整行、整列删除——**操作方法：hover 表格左侧/上方的 ⠿ 拖拽 handle，点击选中整行/整列 → 浮动工具栏出现「删除行/列」按钮；若按钮出现但无效请报现象**）（反馈：能看到工具栏了，但是工具栏样式没有适配咱们的风格。*本轮已修：button-group 直写 --vscode-* 变量（浮层背景/边框/18px 图标/hover 高亮），commit 28b8d29*）

## 2. #16 大文档卡顿修复

* [x] 打开 ≥1000 行文档连续输入，无卡顿
* [x] 中文拼音组合期间流畅，空格确认立即上屏
* [x] 保存后未改动行保持原样（git diff 最小）

## 3. #15 Clean Markdown（默认 clean 模式）

* [x] 编辑含 `operation_log_id`、`foo[0]` 等标识符的文档，保存后无多余转义（反馈：但在块尾编辑一次就会退到块外——**上游 #2413 已知限制，本版不可修**）
* [x] 表格编辑后无冗余 `<br />`
* [x] 切 `compatible` 模式后行为与旧版一致——**操作方法：设置 epytor.markdown.serializationMode = compatible → 编辑保存 → 观察转义比 clean 多（与 1.1.6 一致）→ 切回 clean**
* [x] 代码块/行内代码/Mermaid/LaTeX 内容不受影响

## 4. Milkdown 7.22.1 回归

* [x] 行内代码输入/退出正常（反馈：现在是不退出了，但通过左右键标记在块内块外也没了，导致永远没法退出了）
* [x] 行内样式尾部退出（上游 #2413）——记录是否有改善（反馈：现在是不退出了，但通过左右键标记在块内块外也没了，导致永远没法退出了）
* [x] 表格单击/拖拽行为不变（#2414 相关）
* [x] 有序列表编辑正常（#2415 相关）

## 5. FindBar 正则搜索

* [x] `Ctrl+F` → 点 `.*` 开关，`\d+` 匹配所有数字并计数
* [x] `Aa` 在正则模式下正确控制 i flag——**操作方法：正则模式输入 `epytor` 搜不到大写 → 关闭 Aa（不区分大小写）后能搜到 `EPYTOR`**
* [x] 输入 `(` 显示「无效的正则表达式」红框，修正后恢复（说明：英文半角 `(` 才触发；中文括号是普通字符，属正常行为）
* [x] 输入 `.*` 不卡死；普通模式行为不变
* [x] 反馈：搜索框按钮tooltip超出屏幕右侧边界——**已修（tooltip 视口兜底），复测确认（反馈：最后两个按钮的tooltip依然超出画面范围被遮挡。本轮再修：横向钳制抽纯函数 + 显示后读回校验、仍超右缘改 right 锚定，commit c3bffb5）**

## 6. 表格网格选择器

* [x] 点击顶栏表格按钮弹出 8×8 网格，hover 高亮 + 标题计数正确
* [x] 点击 3×4 插入 3 行 4 列表格，光标进入首单元格
* [x] Esc / 点击外部 / 滚动关闭；重复点击无重复面板
* [x] 亮暗主题样式正常

## 7. 标题吸顶 + 折叠

* [x] 长文档滚动：吸顶条显示当前章节，跨标题切换 + 推挤过渡（反馈：第一次滑动后，再滑回顶部，吸顶标题无法消失，相当于有两个H1。**本轮已修：根因 = heading.css 的 display:flex 覆盖 hidden 属性 UA 样式，hideSticky 无效；JS 显式 display + [hidden] CSS 兜底，commit 8433746**）
* [x] 点击吸顶条跳回标题（反馈：可跳回，但会从顶部工具栏漏出一点再往前的标题吸顶）
* [x] hover 标题出现箭头，折叠后内容隐藏；切换文本编辑器确认文档内容无损
* [x] 折叠标题尾部按 Enter 展开
* [x] 编辑增删标题后折叠状态正确
* [x] TOC/行定位/网格选择器无回归

## 8. 表格换行

* [x] 单元格内 Shift+Enter 换行，继续输入后保存 → 源码为 `x<br />z`（**本轮已改闭环：序列化输出 GFM 标准 `<br />`（GitHub 渲染为换行）；加载时 Extension 把表格内 `<br />` 转 `&#10;` 绕过 remark-gfm 解析层丢弃 `<br />` 的上游 bug，渲染往返一致，commit 4bca061**）；换行后不输入直接保存 → 不产生多余换行（反馈：换行如果下面是空行，无法渲染，源码无变化，返回渲染后空换行消失，但这个算不算问题需要讨论——**讨论结论：维持现状（CommonMark 段落尾 hardbreak 标准语义，GitHub 同样行为，尾随换行不保存）**）
* [x] 单元格外 Shift+Enter 行为不变
* [x] `tableWrapMode` 切换生效——**2026-09-07 用户决策：三档简化为两档——wrap（=旧 aggressive，任意字符断行，默认）/ nowrap（=旧 none，不换行 + 表格横向滚动）；旧配置值自动迁移（normal/aggressive→wrap，none→nowrap）。另：全部 CSS 规则的选择器已从失效的 .milkdown-table-block 前缀改到真实 DOM（.milkdown .ProseMirror table），此前多轮「三档无区别/none 无滚动/normal 不换行页面变宽」均为选择器落空所致**

## 9. Frontmatter 可编辑

* [x] 面板 key/value 直接编辑，保存后源码 YAML 头同步、正文不变
* [x] 加行/删行正常；全删后 YAML 头消失（反馈：编辑行可以，新增行无效——**说明：新增行必须填写 key 才会写盘（空行被过滤）；点击 + 后填 key/value，失焦或切模式立即保存**）（反馈：加行依然无效，出现新bug，加行后，前面多出一个蓝色小框。**说明：蓝色小框 = 新行输入框的 focus 指示（正常交互）；诊断日志证实：消息链路完全正常，你输入的内容落在了上一行（author）的 value 框——点 + 后焦点未进入新行；本轮已修：点 + 后双保险聚焦新行 key 框（同步 + 下一宏任务补一次）。复测：点 + → 观察光标是否在新行 key 框闪烁 → 输入 key → 输入 value → 失焦 → 看源码**）（反馈：加行依旧无效）
* [x] 含引号/冒号/URL 的值往返一致（说明：frontmatter 的 URL 值是纯文本，非可点击链接——正常形态）
* [x] 切换文本编辑器/预览往返状态一致；外部修改文件后面板刷新（反馈：编辑行可以，新增行无效——**新增行已修（失焦立即保存），遮挡问题下一批修复**）（反馈：加行依然无效。**本轮核查：flushSave→notifyFrontmatterUpdate→Extension 更新→写盘链路完整；请按「点 + → 填 key → 失焦」操作并看源码 YAML 是否出现新行；若仍无效请描述具体现象**）（反馈：新问题，源码/渲染切换后，点击frontmatter内编辑行，页面会跳到下面标题行。另外，刚才没注意点哪里了，也会突然跳到mermaid处，我没法复现了。**本轮已修：根因=切回预览的「定位到文本编辑器光标行」在 1 万行文档渲染慢时延迟 300-2000ms 执行，用户已开始交互时突然跳动；延迟定位加用户交互保护，交互后放弃（commit 54c7825）**）

## 10. Mermaid 预览缩放

* [x] 预览 hover 出现控制条，＋/− 在 0.4–3× 范围缩放，× 重置（反馈：默认依旧过大，缩到最小反而是最合适的，最小和最大的区间明显不合适。**本轮已修根因：此前百分比相对容器而非原始渲染宽度，宽容器下默认反而放大；改像素基准 1×=原始大小，commit 0e7eafe。默认 0.8/区间 0.2–3 是否仍不合适请按新包复测后确认**）
* [x] 放大后可横向滚动查看
* [x] 深浅主题切换后图表重绘、控制条仍在
* [x] 全屏（既有）与缩放共存正常（反馈：工具栏在，但三个按钮都无效）

- [x] 其他反馈：toc里TABLE OF CONTENT字体依然过大。
- [x] 顶部工具栏在宽度变小后，more和其他按钮重叠（反馈：问题依旧，并且工具栏内按钮并不会根据宽度自动隐藏，more按钮点击没有反应，同时tooltip右边超出画面不可见，与搜索框tooltip问题相同。**本轮已修三处根因：①「⋯」按钮 display:inline-flex 覆盖 hidden 属性——宽窗口无需溢出时按钮仍显示且点击无反应 ② 可用宽度未扣顶栏左右内边距 125px——按钮溢出与 more 重叠 ③ mutation observer 自触发死循环；commit f2fcc16**）（反馈：和最后一个按钮依然重叠，里面按钮部分又变成黑色。**本轮已修：①「⋯」改为动态定位紧跟按钮组右缘（不再固定视口右缘，根除重叠）② 菜单图标 fill:currentColor 统一——官方图标无 fill 属性默认黑色，Tb 图标内联 fill:none!important 保持描边（上轮 svg * 规则曾把齿轮填实心）**）（反馈：变窄后，在前几个被碰到的图标依然会有重叠，随变窄问题逐渐消失。**真实引擎复现：临界宽度下 top-bar-inner 边界与按钮实际占位错位 150px+；本轮已改「⋯」直接跟随最后一个可见按钮右缘（commit f69170d），复测确认**）（反馈：缩小到水平线按钮和引用块按钮依然会重叠，more菜单右边缘也会被页面边缘压住一部分。**本轮已修：①「⋯」位置直接观察 topBar 尺寸变化（resize 触发缺失时重排滞后）② 菜单右缘 clamp 防被页面边缘压住（commit 54c7825）**）（反馈：问题又出来了）
- [x] 顶部工具栏more按钮点击后，失去页面编辑焦点，但光标还在（反馈：点击无反应）
- [x] 新问题：图片编辑，ALT/CAP下输入框看不见光标，深色浅色模式都看不见。**本轮已修：caret-color 显式指定 input 前景色 + 输入框 min-width 140px（commit 54c7825）**
- [x] 新问题：大文件我把行数增加到1万行了，中文输入卡顿严重，英文输入也肉眼可见卡顿。**本轮已修：吸顶条缓存重构——此前每次输入/滚动全量测量所有标题 rect（几百标题 × 每帧），改文档坐标缓存 + 300ms 防抖重建，滚动纯数字计算零 DOM 查询（commit 54c7825）；复测若仍卡请描述卡顿时机（输入中/滚动中/拼音组合中）（明示3000行已知限制）**
- [x] 新问题：切换到别的文件，再回到epytor页面，光标焦点失去，光标在，但输入无效。**本轮已修：切回时自动恢复编辑器焦点（commit 54c7825）（反馈：切换文件焦点必失）**
- [x] 老问题又出来了：表格整行列删除工具栏，样式又回去了，不是咱们epytor的样式风格。

