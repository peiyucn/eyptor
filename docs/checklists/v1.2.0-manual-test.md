# v1.2.0 手测清单

> 测试包：`releases/epytor-vscode-1.2.0.vsix`
> 使用方法：每项标注 ✅ 通过 / 🛑 有问题（附现象），最后汇总反馈。

## 1. 死代码删除回归

* [x] 选中文字 → 官方浮动工具栏正常弹出（粗体/斜体/删除线/链接等）
* [ ] 表格操作正常：单元格光标、行列插入/删除按钮（反馈：但无法整行、整列删除——**操作方法：hover 表格左侧/上方的 ⠿ 拖拽 handle，点击选中整行/整列 → 浮动工具栏出现「删除行/列」按钮；若按钮出现但无效请报现象**）（反馈：能看到工具栏了，但是工具栏样式没有适配咱们的风格。）

## 2. #16 大文档卡顿修复

* [x] 打开 ≥1000 行文档连续输入，无卡顿
* [x] 中文拼音组合期间流畅，空格确认立即上屏
* [x] 保存后未改动行保持原样（git diff 最小）

## 3. #15 Clean Markdown（默认 clean 模式）

* [x] 编辑含 `operation_log_id111111`、`foo[0]` 等标识符的文档，保存后无多余转义（反馈：但在块尾编辑一次就会退到块外——**上游 #2413 已知限制，本版不可修**）
* [x] 表格编辑后无冗余 `<br />`
* [x] 切 `compatible` 模式后行为与旧版一致——**操作方法：设置 epytor.markdown.serializationMode = compatible → 编辑保存 → 观察转义比 clean 多（与 1.1.6 一致）→ 切回 clean**
* [x] 代码块/行内代码/Mermaid/LaTeX 内容不受影响

## 4. Milkdown 7.22.1 回归

* [ ] 行内代码输入/退出正常（反馈：在块尾编辑一次就会退到块外）
* [ ] 行内样式尾部退出（上游 #2413）——记录是否有改善（反馈：在块尾编辑一次就会退到块外）
* [x] 表格单击/拖拽行为不变（#2414 相关）
* [x] 有序列表编辑正常（#2415 相关）

## 5. FindBar 正则搜索

* [x] `Ctrl+F` → 点 `.*` 开关，`\d+` 匹配所有数字并计数
* [x] `Aa` 在正则模式下正确控制 i flag——**操作方法：正则模式输入 `epytor` 搜不到大写 → 关闭 Aa（不区分大小写）后能搜到 `EPYTOR`**
* [x] 输入 `(` 显示「无效的正则表达式」红框，修正后恢复（说明：英文半角 `(` 才触发；中文括号是普通字符，属正常行为）
* [x] 输入 `.*` 不卡死；普通模式行为不变
* [ ] 反馈：搜索框按钮tooltip超出屏幕右侧边界——**已修（tooltip 视口兜底），复测确认（反馈：最后两个按钮的tooltip依然超出画面范围被遮挡）**

## 6. 表格网格选择器

* [x] 点击顶栏表格按钮弹出 8×8 网格，hover 高亮 + 标题计数正确
* [x] 点击 3×4 插入 3 行 4 列表格，光标进入首单元格
* [x] Esc / 点击外部 / 滚动关闭；重复点击无重复面板
* [x] 亮暗主题样式正常

## 7. 标题吸顶 + 折叠

* [ ] 长文档滚动：吸顶条显示当前章节，跨标题切换 + 推挤过渡（反馈：文档最上方出现比较靠下的章节标题，明显bug，点击吸顶标题后，再往上一个吸顶标题出现在顶部工具栏后面，遮挡住大部分，漏出一点点能点击）
* [x] 点击吸顶条跳回标题（反馈：可跳回，但会从顶部工具栏漏出一点再往前的标题吸顶）
* [x] hover 标题出现箭头，折叠后内容隐藏；切换文本编辑器确认文档内容无损
* [x] 折叠标题尾部按 Enter 展开
* [x] 编辑增删标题后折叠状态正确
* [x] TOC/行定位/网格选择器无回归

## 8. 表格换行

* [ ] 单元格内 Shift+Enter 换行，继续输入后保存 → 源码为 `x&#10;z`（已改为实体闭环——remark-gfm 解析层丢弃 `<br>`，实体往返一致；旧 `<br>` 文件加载丢失仍为上游限制）；换行后不输入直接保存 → 不产生多余换行（反馈：编辑后进入源码页正常，再回编辑页换行失效，但br在源码依然存在——**已修，复测确认**）
* [x] 单元格外 Shift+Enter 行为不变
* [ ] `tableWrapMode` 三档切换生效——**三档含义：normal = CJK 不拆词、长词折行；aggressive = 任意字符断行；none = 不换行 + 横向滚动。最明显差异在 none 档（3.4 节 URL 行不换行、表格横向滚动）**

## 9. Frontmatter 可编辑

* [x] 面板 key/value 直接编辑，保存后源码 YAML 头同步、正文不变
* [ ] 加行/删行正常；全删后 YAML 头消失（反馈：编辑行可以，新增行无效——**说明：新增行必须填写 key 才会写盘（空行被过滤）；点击 + 后填 key/value，失焦或切模式立即保存**）
* [x] 含引号/冒号/URL 的值往返一致（说明：frontmatter 的 URL 值是纯文本，非可点击链接——正常形态）
* [ ] 切换文本编辑器/预览往返状态一致；外部修改文件后面板刷新（反馈：编辑行可以，新增行无效——**新增行已修（失焦立即保存），遮挡问题下一批修复**）

## 10. Mermaid 预览缩放

* [ ] 预览 hover 出现控制条，＋/− 在 0.4–3× 范围缩放，× 重置（反馈：按钮有效，但默认太大，最小也不够小，恢复按钮不该用叉子）
* [x] 放大后可横向滚动查看
* [x] 深浅主题切换后图表重绘、控制条仍在
* [x] 全屏（既有）与缩放共存正常（反馈：工具栏在，但三个按钮都无效）

<br />

* [ ] 其他反馈：toc里TABLE OF CONTENT字体依然过大。
* [ ] 顶部工具栏在宽度变小后，more和其他按钮重叠
* [ ] 顶部工具栏more按钮点击后，失去页面编辑焦点，但光标还在

