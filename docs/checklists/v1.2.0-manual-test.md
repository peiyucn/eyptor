# v1.2.0 手测清单

> 测试包：`releases/epytor-vscode-1.2.0.vsix`
> 使用方法：每项标注 ✅ 通过 / 🛑 有问题（附现象），最后汇总反馈。

## 1. 死代码删除回归

- [ ] 选中文字 → 官方浮动工具栏正常弹出（粗体/斜体/删除线/链接等）
- [ ] 表格操作正常：单元格光标、行列插入/删除按钮

## 2. #16 大文档卡顿修复

- [ ] 打开 ≥1000 行文档连续输入，无卡顿
- [ ] 中文拼音组合期间流畅，空格确认立即上屏
- [ ] 保存后未改动行保持原样（git diff 最小）

## 3. #15 Clean Markdown（默认 clean 模式）

- [ ] 编辑含 `operation_log_id`、`foo[0]` 等标识符的文档，保存后无多余转义
- [ ] 表格编辑后无冗余 `<br />`
- [ ] 切 `compatible` 模式后行为与旧版一致
- [ ] 代码块/行内代码/Mermaid/LaTeX 内容不受影响

## 4. Milkdown 7.22.1 回归

- [ ] 行内代码输入/退出正常
- [ ] 行内样式尾部退出（上游 #2413）——记录是否有改善
- [ ] 表格单击/拖拽行为不变（#2414 相关）
- [ ] 有序列表编辑正常（#2415 相关）

## 5. FindBar 正则搜索

- [ ] `Ctrl+F` → 点 `.*` 开关，`\d+` 匹配所有数字并计数
- [ ] `Aa` 在正则模式下正确控制 i flag
- [ ] 输入 `(` 显示「无效的正则表达式」红框，修正后恢复
- [ ] 输入 `.*` 不卡死；普通模式行为不变

## 6. 表格网格选择器

- [ ] 点击顶栏表格按钮弹出 8×8 网格，hover 高亮 + 标题计数正确
- [ ] 点击 3×4 插入 3 行 4 列表格，光标进入首单元格
- [ ] Esc / 点击外部 / 滚动关闭；重复点击无重复面板
- [ ] 亮暗主题样式正常

## 7. 标题吸顶 + 折叠

- [ ] 长文档滚动：吸顶条显示当前章节，跨标题切换 + 推挤过渡
- [ ] 点击吸顶条跳回标题
- [ ] hover 标题出现箭头，折叠后内容隐藏；切换文本编辑器确认文档内容无损
- [ ] 折叠标题尾部按 Enter 展开
- [ ] 编辑增删标题后折叠状态正确
- [ ] TOC/行定位/网格选择器无回归

## 8. 表格换行

- [ ] 单元格内 Shift+Enter 换行，继续输入后保存 → 源码为 `x<br>z`；换行后不输入直接保存 → 不产生多余换行
- [ ] 单元格外 Shift+Enter 行为不变
- [ ] `tableWrapMode` 三档切换生效（CJK 词/长 URL/横向滚动），表头同效

## 9. Frontmatter 可编辑

- [ ] 面板 key/value 直接编辑，保存后源码 YAML 头同步、正文不变
- [ ] 加行/删行正常；全删后 YAML 头消失
- [ ] 含引号/冒号/URL 的值往返一致
- [ ] 切换文本编辑器/预览往返状态一致；外部修改文件后面板刷新

## 10. Mermaid 预览缩放

- [ ] 预览 hover 出现控制条，＋/− 在 0.4–3× 范围缩放，× 重置
- [ ] 放大后可横向滚动查看
- [ ] 深浅主题切换后图表重绘、控制条仍在
- [ ] 全屏（既有）与缩放共存正常
